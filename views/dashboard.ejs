<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <%- include('partials/head', { titulo: 'Painel' }) %>
</head>
<body>
  <div class="app">
    <%- include('partials/sidebar', { paginaAtual: 'dashboard' }) %>

    <div class="conteudo">

      <%- include('partials/flash') %>
      <div class="topo-pagina">
        <div>
          <h1>Painel</h1>
          <p>Resumo do dia e entregas em aberto</p>
        </div>
      </div>

      <div class="grade-cards">
        <div class="card destaque-chama">
          <div class="rotulo">Botijões cheios em estoque</div>
          <div class="valor"><%= estoque.qtd_cheios %></div>
        </div>
        <div class="card">
          <div class="rotulo">Galões de água cheios</div>
          <div class="valor"><%= estoqueAgua.qtd_cheios %></div>
        </div>
        <div class="card destaque-verde">
          <div class="rotulo">Vendas hoje (gás + água)</div>
          <div class="valor"><%= resumoHoje.total_vendas %></div>
        </div>
        <div class="card destaque-verde">
          <div class="rotulo">Faturado hoje</div>
          <div class="valor">R$ <%= Number(resumoHoje.total_valor).toFixed(2) %></div>
        </div>
        <div class="card">
          <div class="rotulo">O.S. aguardando entrega</div>
          <div class="valor"><%= osPendentes %></div>
        </div>
      </div>

      <div class="painel">
        <div class="painel-titulo">
          <span>Aguardando botijão/galão vazio (<%= pendentes.length %>)</span>
        </div>
        <% if (pendentes.length === 0) { %>
          <div class="vazio-lista">Nenhuma entrega pendente. Tudo certo por aqui.</div>
        <% } else { %>
          <table>
            <thead>
              <tr>
                <th>Produto</th>
                <th>Qtd.</th>
                <th>Cliente</th>
                <th>Pedido</th>
                <th>Observação</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% pendentes.forEach(function(v) { %>
                <tr>
                  <td><%= v.produto === 'agua' ? 'Água' : 'Gás' %></td>
                  <td><%= v.quantidade %></td>
                  <td><%= v.cliente_nome || 'Sem cliente cadastrado' %></td>
                  <td><a href="/pedidos/<%= v.pedido_id %>"><span class="selo-os">Nº <%= v.pedido_id %></span></a></td>
                  <td class="texto-suave"><%= v.observacao || '—' %></td>
                  <td>
                    <form method="POST" action="/pedidos/<%= v.pedido_id %>/itens/<%= v.item_id %>/confirmar-vazio">
                      <button class="botao botao-pequeno" type="submit">Confirmar retorno do vazio</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>
